# v12 預測成本覆蓋修訂

2026-09-15 台灣時間。使用者要求繼續改善實際 Demo 獲利；本次修訂為 `forecast-net-edge-v1`，保留 v12 原始各 30 次目標及所有虧損。它明確取代先前只要求預測方向有利、以 ATR 目標通過成本檢查的設計。

16:31:20 UTC 的完整成交輸入顯示 v12 現貨 7 筆、合約 10 筆均已平倉，合計淨損 5.44541199 USDT。17 筆中有 14 筆的進場時預測幅度不足估計來回成本，16 筆不足原成本加 30 bps。這是原始計畫的算術查核，包含贏單與輸單；不能把被新規則排除的舊交易當成省下的虧損或新版績效。

BNB 合約 44 為例：預測幅度 3.614847 bps、估計來回成本 18.138374 bps、含原緩衝門檻 48.138374 bps；原 2 ATR 目標約 51.31 bps，因此舊規則放行。實際淨損 0.4877776 USDT。ATR 距離不能代替模型預測的報酬幅度。

新進場必須讓最後一根真實模型預測相對當時可成交 ask/bid 的有利幅度，嚴格超過完整估計成本加既有 30 bps 緩衝。相等或不足就 HOLD；不改預測、不放大模型數值、不降低成本。通過者依預測幅度減完整門檻排序，分數仍不是校準後的期望獲利。已收盤 5m/15m 動量、三根方向、品質抑制、同輪證據、原 ATR 退出、1 USDT 風險與其他金額限制保留。

主程式提案與即時報價重查均執行此條件。原生 guard v4 在 callback、context、CCXT wire 階段重算；拒絕舊 policy 混入新進場。價格變動造成成本不足屬正常 filtered，不建立 pending，不累積程序故障或停止模型。舊持倉退出計畫仍可讀取。實際成交以三個 entryPolicyCohorts 分開，原總損益及計數不重設。

驗證：459 JS、515 Python、131 檔語法檢查通過，隔離 native smoke 完成開平倉且 realOrders=0。測試包含多空成本邊界、足夠 ATR 但不足預測、即時報價漂移、連續 filtered 不停模型、CCXT 節流後再次拒絕成本不足、舊計畫退出相容及版本損益保留。

現貨與合約於 16:39:36 UTC 恢復 watch，16:40 新週期已核對 64 份部署來源、實際程序與 guard v4。現貨 10 個預測但品質檢查抑制進場；合約該輪 1/4 個預測可用，BNB 被新成本門檻排除，其餘三對缺模型預測。沒有新成交，不能宣稱已提高勝率。維護跳過一次 16:35 交易週期，模型持續運作；原生退出保留，未取消券商訂單。16:29 模型曾由既有 watchdog 同 pin 恢復，發生於本次維護之前，不是這次策略更新重啟模型。

證據目錄：`local/v12-net-edge-2026-09-15/`。包含 `entry-cost-audit.json`、`running.json`、`validation.json`、`validation-addendum.json`、測試紀錄、修改前後來源與第一個新週期。validation 原模板的 missedScheduledBoundary=null 不能解讀為零跳過週期，實際維護空檔以 addendum 為準。後續依實際扣費成交淨利、平均盈虧、回撤與交易可得性評估，不因測試通過便宣稱獲利改善。
