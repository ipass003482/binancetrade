# v9：突破後回測並收回，實際 Demo 驗證

2026-09-11 晚間使用者再次要求主動改善虧損。本輪在原有自主 Demo 授權下，提前結束 v8 的 72 小時量能比較，啟用獨立 v9 假設。舊窗口未完成，不能宣稱選出最佳倍數。新進場為 `atr15m-forward-v9`，原生引擎 `demo-rule-exits-v9`，提示詞版本 9。

## 改動及判定

相對原版 v8（量能 0.8），只新增確認 K 回測條件：前一根完成 5m K 突破其前 12 根區間，加上原有 0.1 ATR5 緩衝；下一根完成 K 多單最低價須觸及或低於固定突破門檻，收盤仍高於門檻，空單反向。不能用突破 K 本身的影線冒充下一根的回測；未回測則 HOLD `DEMO_BREAKOUT_RETEST_REQUIRED`。以固定門檻檢查，不重算移動門檻，不使用未完成 K。

目的：檢驗回測確認能否減少追價後反轉與成本損失。維持每 5 分鐘評估、1h 方向及 SMA8/20、4h 參考、0.8 相對成交量、15m ATR 停損 1／停利 2、最長 4 小時、每筆含成本風險 1 USDT 與淨浮盈 0.50 USDT 後含費保本。現貨做多、逐倉合約多空。每日次數不限；金額、部位、每日虧損與共同回撤保護保留。

原理參考 [CME 支撐與阻力教材](https://www.cmegroup.com/education/courses/technical-analysis/support-and-resistance)：突破後原阻力可能成為支撐，反向亦然；教材也說明這是區域而非必然準確的價格。精確回測門檻是本專案可測試的規則定義，並非該來源推薦的最佳參數或獲利證據。

v9 的 0.8 是原版基準，不是由本日小樣本選出的勝者。與上一個 vol-10 時段相比同時存在量能和回測條件差異，不能將結果全歸功於回測。先完整累積實際成本後成績，分列淨利、平均盈虧、PF、回撤、退出原因及被拒絕訊號；30 筆是初步比較參考，正式研究沿用既有 100 筆／30 UTC 日／PF1.2 門檻及成本壓力檢查，不降低門檻，也不以兩筆輸贏頻繁調參。即使達到研究門檻仍需月份證據，沒有自動轉真金。

## 實際依據與限制

23:18:25 台灣時間的實際帳本：v8 現貨 12 筆全部平倉，淨 -0.57065012 USDT；合約 3 筆全部平倉，淨 -1.30304544，合計 -1.87369556。全版本當日平倉 -9.31046279；共同 2000 USDT 基期累计 -10.82720987，淨資產 1989.17279013，最大已取樣回撤 14.14340793。精確輸入封存在 `local/readiness/2026-09-11T15-18-26-893Z-e50a318a/`。

`local/v9-retest-entry-audit.json` 僅重讀 15 筆既有成交當時的行情快照，检查新條件是否成立：6 筆不符合，其中也含 2 筆贏單。因此不能說「只去掉虧損單」，不能把歷史重分類加總成 v9 收益，也不能推算空出的資金後來一定會賺。這份資料只支持進行下一個可歸因的實際試驗，不能證明改善。

保本實例 NEAR 現貨 47：原生浮盈達約 0.5069634 USDT 時啟動、停損價 2.668，實際平倉均價 2.665，淨 -0.0411879。觸發保本不等於成交淨利必為零，後續持續核對成交與費用，不虛構收益。

## 部署及稽核

23:19:31 透過既有 stop 暫停兩邊進場，退出引擎持續管理。23:30:17 再次確認兩邊空倉、未知委託為零；完整歷史現貨 49／合約 28 筆（含所有舊版）。`local/v9-deploy-baseline/` 保留舊 trial／report、history、journal、保護證據、STOP、所有進場計畫 SHA256、共同基期及完整取樣；SQLite 一致性備份兩邊 integrity_check 均通過。

新 trial 於 23:30:18 建立並排除全部先前交易；旧 trial/report 另有帶 v8 版本名的封存。共同基期與虧損沒有重設。原量能設定僅 disabled，原起點保留，不再自動切換下一個 1.2 時段。舊 v8 所有既有退出計畫仍受原生相容性保護。來源修改前副本在 `local/v9-source-backup/`。

已驗證：334 項 JS、199 項 Python 全通過，118 檔語法檢查及 policy 有效；原生 Freqtrade 隔離 dry-run 開平倉 smoke 通過、正式訂單 0，封存 `local/native-smoke/23d0746e-cdaf-4efd-ab4b-b2160f435dd8/`。包含多空精確觸及、未回測拒絕、錯誤 K 影線不採用，以及 bridge 拒絕不符合回測的提案且不送單；v8 既有退出相容也通過。這是工程驗證，不是獲利成果。

既有 `binance-demo` 追蹤已改成 v9，保留每小時檢查及午夜結算後恢復每 6 小時的原收尾安排。舊 v8 當日目標仍分列現貨 12/35、合約 3/35，v9 成交不能補計進去。後續檢查以本文件最新部署核對及 `local/readiness/latest.md` 為準。

## 已完成恢復：23:36 核對

23:33:38 兩模式 API 均回報原生 v9、未知提交為 0、assertNativeProtection 通過、全平。23:34:13 完成兩邊 cli resume；新的 watcher 於 23:34:19 啟動。23:35:11 兩邊首輪完成，均 HOLD、連續失敗 0、沒有 entryWait 或資料錯誤，下一輪 23:40:05。此次維護暫停 23:19:31–23:34:13，涵蓋每模式 23:20／23:25／23:30 三個評估窗口，不補造訊號或收益。

首輪 `snapshot`／`analysis`／`rules`／`prompt-contract` 與提示詞 SHA256 一致，量能分組為 null、門檻 0.8、confirmationRetestRequired=true、llmInvoked=false。首輪 source fingerprint 與 `local/v9-deployed-source/manifest.json` 相符，61 份實際部署來源有精確副本和 SHA256；現貨 fingerprint `5f7b3fad90d185079b2c7eca3729d8f6816f3a60cdd3cb96ee929705266d45ef`、合約 `06d527f7bea3838464000e83ab961cf11004b67cfdfb07a5bf54292a983b49ac`。

`local/v9-first-cycles.json` 確認所有部署前進場計畫雜湊和共同基期完全不變；完整歷史 portfolio samples 與 orders journal 仍為現有檔案的原始前綴，僅正常追加。`local/v9-running-verification.json` 於 23:35:37 確認兩模式未 STOP、保護檢查通過、未知 0、持倉 0；共同累積 -10.82720987、最大取樣回撤 14.14340793 USDT、證據完整。新版尚無成交，淨利 0 是尚未交易，不是已獲利。

23:36:07 supervisor 確認兩模式 RUNNING、watch 與 cycle 均新鮮。新的原生 owner 現貨 23460／合約 16072；watcher 24292／24648；supervisor 10496；dashboard 5188。唯讀 dashboard 兩邊顯示 v9、rulesReady=true、無帳戶或報告讀取錯誤。月度準備報告仍保留全部舊版本分組與虧損，v9 樣本不足；未啟用真金。

[23:45 追蹤](demo-check-2026-09-11-2345.md)：23:40 新增第一筆 v9 ADA 現貨；23:46 仍持有 255.2 ADA、浮虧 -0.54283592 USDT，原生完整停損確認；合約空倉。兩邊各三個新週期無失敗，v9 尚無平倉，本輪維持同一假設與參數。停電後等週日使用者實際重啟，週六不提前復原交易。

[9/12 00:46 與 9/11 結算](demo-settlement-2026-09-11.md)：ADA 已於 9/11 23:51 停損，淨 -0.92525312 USDT，v9 首筆已平、合約仍無成交。兩邊目前空倉，各 15 個新週期無失敗，參數不變；共同累積 -11.75246299。9/11 收尾完成，既有追蹤已恢復每 6 小時；維護安排照舊。

[9/12 06:51 核對](demo-check-2026-09-12-0650.md)：新增 ADA 現貨虧損 -0.876707，SOL 合約多空兩筆保本退出合計 +0.02090352；現貨與合約各 2 筆 v9 平倉，共 -1.78105660。兩模式目前空倉，各 88 週期無失敗；共同累積 -12.60826647。已核對保本觸發、回執及收益回吐，參數與維護安排不變。
