# 現貨與合約各 30 次 v11 DEMO 成交

使用者於 2026-09-14 再次要求 30 次交易，並明確選擇「持續跑到各 30 次」，不設今日午夜期限。本輪各自計算現貨 30 次、合約 30 次新的實際策略開倉；保持既有 DEMO 自動交易授權。

## 固定起點與計數

- 起點為完整帳戶讀取完成時間 2026-09-14T02:58:25.478Z，台灣時間 10:58:25.478。
- 不變任務檔：`local/trade-goals/2026-09-14-v11-30-each/goal.json`。原始證據另存 `baseline-input.json`，SHA256 為 `03d9bd0fea2357764f27efa09983aef5bd5d56d3c02b0f68007ed01d75c99e90`。
- 起點已有現貨 56 筆、合約 34 筆，均已平倉，全部排除於本輪新成交計數。新任務起始進度各為 0/30。
- 固定策略 `kronos-forward-v11`、模型指紋 `0bc0a246933d625d8c7724347a0dcc8cf8619d3646f46565558607b6a0cf96d1`。不可偷偷混入新版本或其他模型；若後續試验更版，另列其目標與成績並明確報告原輪狀態。
- 每個 mode + trade_id 只計一次。必須同時有正確版本／模型的唯一策略進場意圖、submitted 或 reconciled 紀錄，以及交易所實際完成的進場成交證據。平倉、分批成交、HOLD、未成交委託、技術驗證單均不能增加筆數。歸因衝突或缺少證據不能宣布達標。
- 同時列出已平倉數、尚持有數、已實現淨損益與浮動損益。`profit_abs` 已含引擎記錄的費用／資金費，不重扣。原始交易所佣金逐項核銷不由這份進度報告重建。

## 執行與追蹤

維持每五分鐘模型判斷、現貨做多、逐倉合約多空、原成本與本金風控，以及每日開倉次數不限。30 次是觀測目標，不是每日額度、同時持倉數或自動停止條件。达到後記錄結果並通知；既有部位依原計畫管理，持續 DEMO 授權仍有效。

用 `node scripts/trade-goal-review.mjs` 從獨立 Demo client 的完整 history 與 journal 產出只讀進度：`local/trade-goals/2026-09-14-v11-30-each/latest.md`。每次保存精確輸入与報告，不覆寫任務起點或原策略 trial。讀取失敗須明示 unavailable，不能把舊的成功進度當成目前狀態。

既有 `binance-demo` 追蹤改為每小時檢查本輪成交、實際損益、進場診斷及執行健康，不建立第二個交易程序。若連續兩小時沒有新成交，檢查這段時間的模型輸出、成本條件、資料錯誤與持倉容量，分清未達條件與執行故障；不得只回覆零持倉或強制湊單。可依既有授權修復故障與研究下一個有證據的假設，但要保留原資料、版本與失敗結果。狀態無實質改變時保持安靜；首次新成交、任一模式首次到 10/20/30、整輪達標、重要損益／故障變化才通知，避免重複。

兩邊各達 30 筆後保存一次達標證據及逐筆損益，若仍有未平倉需另追蹤其實際退出。完成後既有監控恢復每六小時，仍 ACTIVE；不自動切換真金。

## 本金帳本

共同本金基準仍為原 2,000 USDT、原始起點 `2026-09-10T11:43:45.501Z`。本輪開始時所有版本共同累計淨損益為 **-11.98755571 USDT**，來源為以上不可變原始輸入。這是固定歷史起點，不是之後查詢時的最新損益；新任務的 0 筆不會清除舊虧損。

9/11 v8 各 35 次任務已另行結算，現貨 12/35、合約 3/35 未達成，這輪不補算。

## 啟用核對

2026-09-14 台灣 11:10:23 最新完整帳戶報告：現貨 0/30、合約 0/30，皆空倉；本輪已實現及浮動損益各 0 USDT，兩邊計數證據完整且無警示。精確輸入與報告位於 `local/trade-goals/2026-09-14-v11-30-each/reviews/2026-09-14T03-10-23-240Z-97804178-52a1-4e9c-836a-48b7094d6bc3/`。

11:10 實際新週期已完成；現貨輸出 10/10，合約輸出 2/4，BTC／SOL 合約因來源修訂已收 K 而拒絕本輪預測，其餘候選未通過成本條件。兩引擎／watch 持續、未知提交 0、未 STOP、每日次數上限 0。與原部署逐檔比對，交易來源雜湊未變；既有策略沒有因增加追蹤工具而重啟或變更。核對證據為 `operations-2026-09-14T03-10-26-466Z-3cd7018c.json`。

391 項 JavaScript 測試通過（含新增 21 項成交計數測試），124 個檔案語法與設定檢查通過。獨立 dry-run 原生開平倉 smoke 通過，`realOrders=0`，此測試不列入本輪 DEMO 策略成交。測試日誌 `js-tests.log`、`native-smoke.log` 位於本任務目錄；原生測試精確產物為 `local/native-smoke/6949786f-a6ac-49aa-b57e-149b2735c4ff/`。

既有自動化已透過工具更新並回讀核對：同一 `binance-demo`、ACTIVE、每小時、原目標對話保留，精確前後設定封存於 `automation-update.json`。追蹤檢查點為 `monitor-checkpoint.json`；任務目標本身不改寫。
