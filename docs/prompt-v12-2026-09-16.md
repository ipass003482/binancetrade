# 提示詞 v12 修訂

更新時間：2026-09-16 14:56（台北）。本次只改善研究提示詞與主機契約的說明，不把提示詞當成獲利保證，也不改寫已成交交易或既有持倉計畫。

## 實際生效來源

- `config/analyst.json` 選擇 `active` 風格。
- `prompts/analyst-active.md` 與 `prompts/analyst-conservative.md` 是現貨研究文字。
- `prompts/analyst-futures-active.md` 與 `prompts/analyst-futures-conservative.md` 是 USDT 永續研究文字。
- `src/analyst.mjs` 將風格、主機限制、帳戶摘要、快照、規則評估與策略契約組合成完整提示詞；`PROMPT_VERSION=12`。
- `src/strategy-contract.mjs` 是 Demo 執行規則的主機契約，不能被快照文字或研究員改寫。

## 這次修正

1. 移除風格檔中與目前 `live-flow-adaptive-v2` 訂單流單一路徑不一致的舊突破、SMA、蠟燭量能、回檔與模型進場要求。
2. 明確分開研究審閱與實際執行：模型只能提出或審閱 schema 動作，主機固定規則、成本、帳戶、風險、時效與原生 guard 才能決定是否送單。
3. 將「現貨、合約各 50 筆」標示為前瞻樣本計數器，不得為湊數強行開倉、增加風險或把預測當成獲利證據。
4. 加入方向能力矩陣。現行 `demo` 是 Binance 一般現貨 Demo，`buy` 建立多單、`sell` 平既有多單；目前沒有借幣／Margin 空單路徑。現貨遇到空方研究訊號時必須 HOLD 並記錄 `SPOT_SHORT_REQUIRES_MARGIN`。`demo-futures` 才接受 `open-short`／`close-short`。
5. 保留目前 v2 的每分鐘訂單流判斷、60 秒證據期限、15 分鐘 ATR 退出距離及 35／70 分鐘波動風險縮放；提示詞不再自行增加進場門檻。

Binance 官方文件說明 Margin 才提供現貨市場的槓桿多空；目前專案的 Demo Spot 傳輸固定在 `demo-api.binance.com/api/`，未接通 Margin 的 `/sapi` 路徑，因此不能把現貨賣出誤報成裸空。若要真正執行現貨空方，需另行驗證並隔離一個 Margin 執行邊界，不能只改提示詞。

## 驗證

`node scripts/check.mjs` 通過（syntax 174、policy valid）；相關提示詞與契約測試 8 項通過；完整 Node 測試 686 項、Python 測試 797 項通過。這些結果證明提示詞／契約一致，不代表策略已獲利。

## 實際載入

2026-09-16 15:03–15:07（台北）已在保留原生停損與持倉的短暫維護窗口重新載入提示詞 v12。`local/prompt-v12-2026-09-16/prepare.json` 記錄 115 個來源的現行雜湊、兩個 Demo 模式的帳戶讀取與零未決提交；`resume-proof.json` 記錄決策 watcher、supervisor 與 model watchdog 已恢復。`local/adaptive-minute-2026-09-16/check-running.mjs` 的恢復後唯讀檢查通過，兩個模式均以 `flow-minute-v1` 每分鐘完成週期。這次維護流程沒有由我們平倉、送出或取消委託；既有持倉計畫仍由原生引擎管理。

同時修正維護腳本的 Windows PowerShell 相容性：`resume.ps1` 不再使用 PowerShell 7 專有的 `ConvertFrom-Json -AsHashtable`，`pause.mjs` 也會一併暫停 model watchdog，避免下一次提示詞重新載入卡在維護檢查。
