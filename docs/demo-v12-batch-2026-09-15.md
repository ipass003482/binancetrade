# v12：同輪不同標的依序執行與 08:00 Demo 目標

使用者要求新策略在台灣 2026-09-15 08:00 前盡力完成 100 筆，並再次要求以扣费後獲利為最終目標。沿用先前明確的各模式目標，這輪按現貨、合約各 100 筆追蹤；已詢問合計或各自，未收到改為合計的指示。

目標檔：`local/trade-goals/2026-09-15-net-edge-100-each-0800/goal.json`。起點 2026-09-14T16:54:52Z，截止 2026-09-15T00:00:00Z。只計起點後、截止前實際成交且可核對模型歸屬的新策略 trade_id；拆單、平倉、測試探針、未成交不加次數。截止後仍追蹤這批持倉的最終損益，Demo 與模型持續運行。原各 30 筆目標、共享 2,000 USDT 本金與全部舊損益保留。

原執行器每 5 分鐘只選一個標的，委託識別只取 snapshotId。這使不同合格標的也無法同輪交易。新增 `per-pair-cycle-v1`：從同一不可變快照依序選擇不同合格標的，識別取版本、snapshotId、pair 的 SHA256 前 32 字元；每一筆保留獨立執行、版本、資金與模型核對紀錄。每次確認回應後重新讀取帳戶及持倉，重算容量與下筆提案；每標的每輪最多一次嘗試。結果不明或拋出錯誤會中止後續委託並保留已送出紀錄，不能重送湊數。

進場 policy 維持 `forecast-net-edge-v1`，固定 Kronos-small 指紋、5m 週期、首分鐘有效窗口、原成本加 30 bps 緩衝、動量、ATR、1 USDT 風險及持倉上限不變。native guard 升至 `kronos-native-entry-v5` 以核對新的委託識別；舊持倉退出計畫仍按原版本。增加處理機會不是提高勝率的證據；模型品質不足或訊號不合格仍不下單，但不得因此停止模型。

驗證：476 項 JavaScript、517 項 Python 通過，136 份語法／設定檢查通過；隔離 native smoke 的 realOrders=0、openTrades=0。測試涵蓋序列資金重查、容量耗盡、重複識別、未知委託停止、native 授權與舊目標歸屬。部署真實狀態以 `local/v12-batch-2026-09-15/running.json` 和 `validation.json` 為準；測試通過本身不表示已啟動。

執行 `node scripts/trade-sprint-review.mjs --goal local/trade-goals/2026-09-15-net-edge-100-each-0800/goal.json` 可取得新目標的實際成交及淨損益；`latest.json`、`latest.md` 和帶來源 hash 的 reviews 留存每次證據。原各 30 筆報表亦持續更新。08:00 若不足，記錄真實不足數，不把晚成交回填到期限內。
