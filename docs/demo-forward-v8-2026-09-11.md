# v8：提示詞與規則對齊，突破後等下一根收盤確認

後續啟用：[成交量倍數 Demo 試驗 v1](volume-forward-v1-2026-09-11.md)。v8 引擎上另有 0.8／1.0／1.2 分時段試驗；下方是原版 v8 的部署紀錄，不能把原始固定 0.8 說明當成試驗期間所有新進場的門檻。以快照的主機契約、進場計畫及分組日誌確認實際設定。

2026-09-11 使用者要求同時改好提示詞與上一輪建議。版本 `atr15m-forward-v8`，原生引擎 `demo-rule-exits-v8`。本輪只有一項進場假設變更：第一次突破後，等待下一根已收 5m K 仍守在原門檻外，再允許進場。保持每 5 分鐘評估；不是改回 15 分鐘進場。部署結果另存 `local/v8-deployment.json`。

前一根為突破 K。用突破 K 之前 12 根的最高／最低價，加／減截至突破 K 的 14 根 ATR5 × 0.1，固定原始門檻。突破 K 收盤必須嚴格越過門檻，下一根確認 K 的收盤也必須嚴格在同一門檻外；等於或收回門檻即取消，影線越過不算確認。確認 K 可盤中回測，無須再創新高／低。現貨做多，合約多空對稱。門檻及 ATR5 不使用確認 K 的數據，避免把確認標準一起移動。

其他條件保留 v7：確認收盤相對 1 小時前方向一致、同側 SMA8/SMA20，4 小時方向僅參考；確認 K 相對前 19 根均量至少 0.8。成本、交易所最小單位、每筆 1 USDT 含成本風險預算、共同 2,000 USDT 基期、金額／持倉風控均保留。兩模式每日開倉次數仍不限。已完成 15m ATR 的 1 倍初始停損／2 倍目標、2% 初始停損比例上限、4 小時最長持有與淨浮盈 0.50 USDT 後含費用保本均不變。

證據：`local/v8-entry-audit.json` 在 2026-09-11T01:46:44.244Z 讀取實際完整成交歷史，核對 v7 UNI、NEAR、SOL 三筆進場時保存的行情。三筆原始時點均只有首次突破，v8 在相同資料下都會等待確認。這只證明新條件改變了原始進場時點，不能據此宣稱避免全部虧損、算出另一條交易路徑的收益，或證實假突破是全部虧損原因。三筆原成交淨損益分別是 −0.52916346、−0.0000744、−0.78872852 USDT，保留在原版本，沒有重算成 v8。

提示詞現在引用 `src/strategy-contract.mjs` 生成的主機契約，參數直接取自執行規則。清除舊 15m 進場、4h 必須同向、成交量 1 倍及再次加計 0.3% 緩衝等衝突描述，明確以實際扣費淨利、共同回撤、月份結果評估。Demo 固定規則負責交易；AI 是研究審閱角色。拒絕僅改配置成 AI 而繞過結構化進場／退出計畫的方式；dry-run AI 研究保留。每輪保存 `.review-prompt.txt`、`.prompt-contract.json`、`.analysis.json`，記錄 `llmInvoked:false`，不把生成提示詞描述成已呼叫模型。`AGENTS.md` 同步加入此工作準則。

每份新進場計畫保存 `entryConfirmation`：突破／確認收盤時間、價格、原始門檻、ATR5 及確認根數。橋接器在真正送單前重算同一確認規則。診斷介面區分「前一根未完成突破」與「確認 K 收回門檻」。原生 v8 支援 v7 舊計畫和原本保本狀態，保留其版本；不回填舊計畫。共同基期和舊進場計畫雜湊另存 `local/v8-source-backup/old-plan-hashes.json`。

部署前現貨因 `DEMO_STOP_CANCEL_UNCONFIRMED` 處於進場暫停；在本輪檢查時交易所對帳已將相關原生停損記為 canceled/closed，`unresolvedStops=0`、`activeStops=[]`，兩個帳戶空倉。恢復前仍需核對完整進場日誌、原生保護、程序身分與新引擎版本；未知送單不重送。停損替換事件與實際成交繼續保存，不刪除原始告警。

評估依後續實際 v8 Demo 成交進行，與 v7 分版本比較扣費淨利、費用、停損退出比例、回撤與已觀測浮盈回吐。確認可能延遲好機會或減少交易，需如實觀測。30 筆作初步比較參考，原有 100 筆／30 UTC 日／PF 1.2 研究門檻照用，皆不是開倉額度。未有新成交時報告「尚無樣本」，不把程式驗證或保存行情比較當成獲利成果。

部署驗證：台灣時間 09:53 啟動兩個 v8 原生引擎，09:54 恢復進場與 supervisor。09:55 首輪兩模式皆完成即時 5m 行情評估，條件未達，結果 HOLD。09:56:10 核對兩模式 health 正常、watch 運作、STOP 不存在、原生保護與程序身分通過；每輪提示詞雜湊和實際決策契約一致，診斷及績效介面都顯示 v8，無資料錯誤。當時兩模式零持倉、v8 尚無成交。完整證據：[v8-deployment.json](../local/v8-deployment.json)。

321 項 JS、189 項 Python、115 個 JS 語法檢查與原生隔離 dry-run 開平倉 smoke 通過。舊進場計畫及共同基期共 22 份雜湊核對無變；兩個 SQLite 備份均通過完整性檢查。共同實驗淨損益仍 −8.95351431 USDT、帳面本金 1991.04648569 USDT、已取樣最大回撤 10.42013605 USDT，未因換版重置。既有 `binance-demo` 每 6 小時檢查提示詞已更新 v8，保持 ACTIVE；月度檢查已重跑，見 `local/readiness/latest.md`。
