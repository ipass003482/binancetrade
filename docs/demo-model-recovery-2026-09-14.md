# 2026-09-14 主模型中斷與復原

使用者要求檢查零持倉及是否有程式問題。本次確認 15:30 台北時間後確實發生新故障，不能全部歸因為成本門檻。

## 故障證據

- `local/model-research/worker.err.log` 在 `2026-09-14T07:30:11.192Z` 記錄 `MODEL_OPERATION_FAILED`；原 Python 程序 13040／21548 均已退出。
- 正式 `status.json` 停在 `07:30:06.023Z`，同時留下可完整解析、時間為 `07:30:11.191Z` 的 `status.json.13040.tmp`，而 `review.json` 已到 `07:30:11.183Z`。高度指向狀態暫存檔取代正式檔時失敗；日誌沒有保留例外類型／errno，不能確定 Windows 錯誤碼或占用者。
- 15:30–15:55 兩模式連續六輪均為 `MODEL_WAIT_TIMEOUT`／`MODEL_EVIDENCE_UNAVAILABLE`。Native 引擎仍完成 HOLD，所以僅看引擎 RUNNING／completed 無法辨認模型已死。
- 固定窗口原始診斷：`local/trade-goals/2026-09-14-v11-30-each/inactivity-2026-09-14T07-55-model-outage-audit.json`，SHA256 `bf71af7c166dab626d3e71444f388f797f88633c95e3fd92ee7d676d84bc43f9`，覆蓋截至 `07:55Z`。
- 15:05–15:25 現貨前五輪仍有 50 筆有效商品預測，35 筆方向不允許、15 筆價格空間不足；合約 15:05–15:20 有 13 筆有效預測且均空間不足，另有三筆來源修訂拒絕。15:25 合約另有一次全部輸入不可用。這些與 15:30 後共同程序中斷分開記錄。

## 已完成的原模型復原

先將原日誌、status／tmp／review／loaded identity 及程序清單封存在 `local/model-worker-recovery-2026-09-14T0758Z/`，避免原 launcher 覆寫日誌後失去證據。核對所有模型素材與四個 implementation 雜湊相符後，使用原 `scripts/start-model-worker.ps1` 重啟，沒有改 inference 程式或 pin。

新 launcher PID 20636、實際 worker PID 21252，建立於 `08:00:31Z`，模型載入於 `08:00:34.440Z`。原 pin 為 `0bc0a246933d625d8c7724347a0dcc8cf8619d3646f46565558607b6a0cf96d1`。16:00 與 16:05 已產生新模型證據；商品完整度與第一分鐘時效以同目錄 `recovery-validation.json` 的核對為準。

## 持續復原的處理範圍

本次補獨立程序監督器；保留原模型實作與固定指紋，不把執行修復偽裝成模型策略改版。它必須區分程序退出、活著但心跳過期、來源過期與正常等待下一輪；只對確定不存在的原程序做有限重啟，先封存證據、核對來源與 pin、尊重 STOP 並限制頻率。活著但異常的程序不得直接殺掉重開。

原 `temp.replace` 無重試的實作仍保留；監督器改善故障後的復原與可見性，並不宣稱已證實或根治 Windows 寫檔衝突。若之後修改原 worker 的寫檔重試／錯誤診斷，必須正常產生新實作指紋並保留舊版歸因。

## 監督器部署與驗證

`src/model-watchdog.mjs` 與 `scripts/start-model-watchdog.ps1` 已於 16:20:49 啟動（PID 19052）。16:21:48 查核主模型與監督器心跳均新鮮、模型來源當前，16:20 兩模式分別有完整 10／10、4／4 商品預測。原 62 份交易部署來源及四份模型實作雜湊不變；精確來源副本與程序狀態在上述復原目錄 `deployed-source/`、`deployment.json`、`final-validation.json`。

- 約每 10 秒檢查；只在原模型程序確定不存在時復原。活著但超過 90 秒無心跳、來源超過兩個五分鐘週期、資料不可用、程序身分不明會分別呈現異常，不殺程序。
- 每 30 分鐘最多三次重啟、至少間隔 60 秒；嘗試前先 flush 預算並保存原故障證據，重查 STOP／程序／pin，再呼叫原 launcher。啟動回覆尚不算復原完成，須等新程序身分與心跳。這是程序重啟限制，不是成交次數限制。
- 預算及紀錄在 `local/model-watchdog/budget.json`、`events.jsonl`、`recoveries/`；監督器自身 STOP、模型 STOP 與兩個交易模式 STOP 均保留。單一模式 STOP 由原模型按模式處理。
- 監督器自身非正常退出可能留下自己的 `worker.lock`；launcher 不自動搶鎖。須先讀 owner 與精確程序／建立時間，確認該鎖已無有效擁有者後，只封存及移除這一個殘留鎖再恢復；不得刪除原模型／lab 的鎖，也不得重設重啟預算。持續 Demo 授權下可由後續代理完成查核，不要求使用者逐次批准例行復原。
- 首次讀取暫時失敗不會製造「已有預算卻遺失」的假狀態；已建立的預算遺失則拒絕重新歸零。模糊啟動結果也計入嘗試，不立即重送。
- 新增 31 項情境測試；全套 JavaScript 423／423 通過，126 份語法／配置檢查通過，原生隔離 smoke 通過且 `realOrders=0`。沒有故意終止正常模型做實帳故障注入。
- 既有每小時追蹤已更新並讀回核對，使用新監督狀態及預算；不再繞過它直接重啟原 worker。`local/goal30-operation-check.mjs` 另列主模型與監督器心跳，且能辨認新週期尚未寫出決策的瞬間，避免誤報檔案缺失。

16:21:44 最新實際帳務仍為現貨、合約各零持倉、各 0／30 次新 v11 成交；沒有未知提交。這幾輪原模型已正常產生預測，HOLD 仍由方向或預測空間不足造成，不能拿先前程序中斷解釋後續所有未成交。

模型 pin、v11 條件、五分鐘週期、各 30 次固定目標、原 2,000 USDT 本金基期及全部歷史損益均保留。恢復預測不等於已成交或改善獲利；後續從真正成交帳務驗證。
