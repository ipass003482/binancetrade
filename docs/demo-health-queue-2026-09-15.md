# Demo 狀態寫入競爭修復

2026-09-15 台灣時間。17:05、17:10 兩次現貨週期在建立新快照前回報 BUSY_OR_STALE_LOCK；週期 lastCycleStartedAt 留在 17:00。背景採樣與交易週期共用 healthUpdate，原本都直接取得獨佔檔案鎖，重疊呼叫可能使交易週期失敗。沒有未知委託或未受保護持倉。

隔離重現：20 次並行健康更新有 19 次失敗，並行事件寫入也失敗。修復只在同程序、同檔案的 health/event 寫入加 Promise 排序；原檔案鎖仍在，外部鎖拒絕、不刪除、不重試委託。失敗後佇列可繼續，所有更新欄位與事件完整保存。新增 3 個測試；544 項 JS 全數通過。Python／策略／風險未改，不重跑無關 Python 測試。

17:10:32 暫停並排空 watch／supervisor；依 PID、命令與建立時間確認 native 後受控替換程序；17:12:32 恢復。維護期間沒有跨過下一個 5 分鐘邊界；故障原先已漏的 17:05／17:10 保留，不能回補假成交。模型未由本次修復停止或重啟。

權威執行證據：`local/v12-health-queue-2026-09-15/` 下 source-deployment.json、running.json、validation.json。每筆原計畫、goal、基期與歷史經過前後核對保留。74 份策略來源與兩模式指紋不變；health.mjs 不在策略指紋內，因此本修復另存 SHA256 並在例行 operation-check 重驗。此前 strength-exit manifest 仍是策略權威。

現貨 16:00–17:07 的 13 輪／130 候選：110 筆未通過 flow（其中 1 筆資料不完整），13 筆淨報酬風險不足，6 筆價格未延續，1 筆通過快照但新報價回落被取消。理由不可混作獨立獲利樣本。新現貨版本仍無實際成交，本次修復不能宣稱策略勝率改善。真實分析保存在 17:07 sprint review 的 heartbeat-analysis.json。

17:15 驗證：現貨 17:15:09、合約 17:15:21 完成新週期，均無連續錯誤、STOP 或未知委託；原生 guard v10 與來源核對通過，模型健康。現貨／合約皆 HOLD，沒有為驗證硬送單。17:15:50 查核今日各 4 筆、均已平倉，淨損益現貨 -0.49769756、合約 +1.64054356 USDT。
