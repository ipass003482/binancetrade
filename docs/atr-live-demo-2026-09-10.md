# 15 分鐘 ATR 候選 B：Demo 實際交易試跑

使用者明確要求候選策略接到 Demo 持續運行，而非只做歷史回測。本次已於 2026-09-10 約 19:03 台灣時間啟用現貨與合約 Demo 的 `atr15m-risk-v4`，實際引擎回報 `demo-rule-exits-v4`、`demo_trading=true`、`running`。這是候選 B 的前瞻 Demo 試跑，不代表先前回測已通過，也不表示已產生成交。

## 啟用內容

- 每 5 分鐘收盤後自動檢查；即時行情及交易仍走既有 Binance Demo 專用目的地與 guarded bridge。
- 維持原 5m 突破／跌破、0.1 倍 5m ATR 緩衝、1h／4h 同向、均線及量能條件。
- 停損、停利改用完整已收盘 15m K 線的 ATR14；仍為 1 ATR 停損（最多 2%）、2 ATR 目標，最多持倉 4 小時。引擎原有 ROI／追蹤保護可能較早退出。
- 每筆預估停損加成本預算上限 1 USDT；依停損距離縮小下單金額，保留既有每單、總曝險、每日進場及虧損限制，合約維持 1 倍。這是計畫風險，跳空或實際成本可能使虧損超過預算。
- 現貨可買入及平倉；合約可做多或做空。未同時啟用候選 C，以保持版本歸因清楚。
- 現貨行情也保留交易所數量與最小金額規格；低於交易所最小下單要求的風險部位會被排除。
- bridge 送單前重算訊號與成本風險。原生 Freqtrade 確認實際下單數量時再檢查新版本計畫、時間、金額及 1 USDT 預算，拒絕引擎因最小金額而向上調整超出預算。
- v1／v2／v3 已保存的退出計畫仍可讀取，舊倉退出時間不會被改寫。

## 執行紀錄

切換前兩個帳戶均為 0 持倉、0 未定送單。先暫停進場，完成更新及檢查，核對舊 Python 引擎程序後重啟；恢復進場及既有自動監控。儀表板已重啟以載入新版本，網址仍為 http://127.0.0.1:18100。

- 現貨首輪行情時間：2026-09-10 19:03:31 台灣時間；snapshot `1aa9787e-2223-4a42-b895-3b9fa37ced92`，HOLD。
- 合約首輪行情時間：2026-09-10 19:03:41 台灣時間；snapshot `ec759920-4f44-44ce-b8de-52fd91ad2198`，HOLD。
- 19:04:37 查核兩邊引擎及 watch 程序存活、未暫停、0 持倉；下一個自動排程為 19:05:05。
- 19:05 自動排程已實際完成：現貨 19:05:12.722、合約 19:05:12.047；兩邊保存 `scheduledCandleBoundary=1789038300000`，決策版本均為 `atr15m-risk-v4`，結果 HOLD。19:05:56 查核健康、未暫停、持倉各 0，下一輪 19:10:05。證據為各模式的 `atr-live-scheduled-check.json`。

這次的績效從 `local/<mode>/atr-live-trial.json` 的 `startedAt` 開始，以 Demo 實際成交及引擎交易紀錄核對。該檔保存啟用前既有 trade ID，避免混入舊版交易。歷史回測損益不寫入此試跑的績效。

首輪只有 HOLD，因此此時還沒有新策略的 Demo 成交盈虧，不能宣稱下單成功或策略已获利。

## 驗證與可追溯檔案

- 173 項 JavaScript、53 項 Python 測試通過，82 個語法檔案檢查通過。
- 原生 Freqtrade 隔離 dry-run 的進出場契約測試通過。它只驗證技術流程，不列入 Demo 策略成交或績效。
- 新增檢查涵蓋候選 B 和離線定義的數值一致性、完整 15m 聚合、風險縮倉、原生下單金額上調與過期計畫拒絕、v4／舊版退出相容性。
- 各模式 `atr-live-before.json`、`atr-live-engine-ready.json`、`atr-live-trial.json` 保存切換證據。
- 各模式 `watch-heartbeat.json`、`health.json`、`runs/*.rules.json`、`runs/*.outcome.json`、`orders.jsonl` 保存持續執行狀態。
- 新進場經 bridge 保存 `entry-plans/<tag>.json`，成交後由引擎持久保存退出計畫。策略版本指紋包含 `src/demo-rules.mjs`。
- 未啟用真實資金交易，未自動提交或推送 Git。
