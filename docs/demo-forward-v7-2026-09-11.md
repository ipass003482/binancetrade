# v7：淨浮盈達 0.50 USDT 後保本

2026-09-11 使用者接受建議並要求完整做好程式。範圍仍為 Binance Demo 虛擬資金；現貨做多、逐倉合約多空。v7 引擎於台灣時間 08:20 首次啟動，補強「已保本後費用異常不得放寬停損」並重跑全部檢查後，最終版本於 08:25 啟動並恢復持續進場。部署與第一輪結果另存 `local/v7-deployment.json`。

本輪假設：在已有浮盈的交易中提早收緊停損，能減少回吐後轉虧。只改退出保護，保留 5m 進場、15m ATR 初始停損／目標、4 小時最長持有、每筆 1 USDT 風險預算及原倉位限制；兩模式每日開倉次數仍不限。0.50 是固定風險預算的 0.5 倍，是待驗證參數，不是從歷史價格套出的獲利。

版本：`atr15m-forward-v7`，原生引擎 `demo-rule-exits-v7`。每份新策略進場計畫固定保存 `profitProtection={version:fee-breakeven-v1,triggerNetUsdt:0.5,riskMultiple:0.5}`。舊 v6 交易仍使用原 ATR 計畫，不加入舊版 ROI／追蹤停損，也不套用 v7 保本。切換前最後一筆 v6 SOL 空單已於 08:08:50 平倉，部署時兩模式空倉。

引擎每輪用 `Trade.calculate_profit(current_rate).profit_abs` 判斷扣費後淨浮盈；納入實際成交数量、進場費、預估平倉費與已入帳資金費。達標後依相同引擎帳務求損益為零的價格，多單向上、空單向下按交易所價格刻度取整。為避免比率／價格浮點轉換每輪多走一格，回傳同一價格刻度區間的中點，再由原生引擎取整。觸發時間、當時淨浮盈、停損價及計算使用的帳務存進交易資料庫 `fee_breakeven`，重啟、浮盈回落及後續成交回呼不會解除或放寬保護。資金費或費率變化只能進一步收緊。

原生交易所保護單依既有 15 秒最短更換間隔撤換，須確認舊單已取消才能建立新單；未知回覆保留對帳，不盲目重送。價格跳過已啟用保本價時，`rules_breakeven` 提供引擎退出路徑。一般行情下由交易所停損先管理，因此成交退出原因也可能是 `stoploss_on_exchange`；不能只看原因字串判斷是否觸發保本。計算或保存資料異常時保留原停損，記錄 `BREAKEVEN_EVIDENCE_INVALID` 並暫停新增曝險。

保本價是停損觸發價，最後損益仍以實際成交為準。現貨保留 STOP_LOSS_LIMIT、限價比例 0.995；合約使用 reduce-only STOP_MARKET。價差、滑價、跳空、未成交或後續資金費仍可能使實際平倉略虧，不把停損價格寫成已實現收益。

部署前證據：`local/v7-final-v6-evidence.json`（08:12:10），v6 現貨 6 筆平倉淨損益 −3.60799019、合約 7 筆 −4.72818948，合計 13 筆 −8.33617967 USDT。共同實驗包含 v5 及驗證單，總淨損益 −7.63554793、帳面本金 1992.36445207、已取樣最大回撤 9.10216967 USDT。共同基期維持 `2026-09-10T11:43:45.501Z`。舊進場計畫逐檔 SHA256、共同基期雜湊與切換前狀態在 `local/v7-restart-before.json`；兩個 SQLite 已備份且通過完整性检查，位於 `local/v7-upgrade-backup/`。v6 forward marker/report 原檔另行歸檔；不清除損失或 equity samples。

驗證：309 項 JS、187 項 Python 全數通過；110 個 JS 語法檢查及原生隔離 dry-run 開平倉 smoke 通過。新增測試覆蓋現貨、多空合約、1x／3x 的金額觸發、費率／正負資金費、實際數量、持久化 SQLite 關閉重開、跳空退出、錯誤資料、已保本後費用異常不得放寬停損、v6 相容，以及實際安裝版本 Freqtrade 的停損替換排程加 guarded adapter／CCXT 序列化。這些離線／dry-run 驗證只證明程式行為，不計入 Demo 獲利。紀錄見 `local/v7-final-{js,python,syntax,native}.log`。

持續驗證以新版本真實成交進行：讀取 trade 的 `fee_breakeven`、原生保護回執與 `forward-report.json.profitReview`，比較每筆淨利、回吐轉虧、保本觸發後實際淨損益、費用與共同回撤。若保本太早導致目標退出减少，以實際觀測指出此代價，不把「假如繼續持有」的價格路徑算成交易收益。既有持續監控在暫停進場時也維持損益採樣。30 筆策略平倉只是初步比較參考，不是開倉額度；不足時如實報告，不標示已驗證穩定獲利或授權真金。

08:27:18 部署核對：兩模式 health 皆正常，supervisor 與 watch 持續運行，交易所保護能力和程序身分驗證通過，無 STOP、無持倉。各完成一輪新版本即時行情評估，均為條件未達的 HOLD；見 `local/v7-first-cycle-demo.log` 與 `local/v7-first-cycle-demo-futures.log`。新版實際策略成交與保本觸發均尚無樣本，不能把原生離線替換測試當成已在交易所觸發保本。共同基期 SHA256 與所有舊進場計畫核對一致，共同淨損益仍為 −7.63554793 USDT。既有每 6 小時檢查已更新到此版本與保本核對程序。
