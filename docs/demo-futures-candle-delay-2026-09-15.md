# 合約 Demo 收盤資料穩定等待

使用者要求改善持續零成交，不能整晚只等待。2026-09-15 台灣 02:04 的固定兩小時稽核（UTC 16:00:59.999–18:00:59.999）顯示現貨、合約各有 23 個完整週期、零新成交，唯一缺少的是已記錄維護中的 16:35 週期。現貨 23 輪皆因模型品質抑制；合約只有 7/23 輪取得全部四個標的的模型輸出。來源 `local/trade-goals/2026-09-14-v12-30-each/inactivity-1800.json`，沒有用後來規則重分類當時決策。

## 可重現的資料問題

UTC 18:00 合約快照的 SOL 最後一根已收 K（17:55 開始）volume=5728.51，而 18:02:54 重新讀取相同時間範圍得到 5732.96。其餘 OHLCV 一致。模型需要同根 OHLCV 與 quote volume 配對，因此原核對正確拒絕不一致資料。

UTC 18:05 在第 5、10、15 秒讀取固定 96 根已收合約 K 線。BTC、ETH、SOL、BNB 四組皆在 5→10 秒間修訂最後一根成交量，其中 ETH、BNB 連收盤價也變動；10→15 秒均一致。18:09 再讀取亦與 15 秒一致。精確 raw JSON、hash、網址與差異在 `local/kline-close-1789409100000/audit.json`。只讀觀測工具在完成 15 秒取樣後因自身舊時鐘快照過期停止，沒有 25/40 秒證據；這個診斷工具錯誤沒有影響交易或模型。後續讀取有各自實際時間，未冒充缺失的取樣。

另外在 18:02 查過現貨 BTC/ETH 及四個合約，rolling 與指定 startTime/endTime 的查詢內容一致；因此沒有證據把問題歸因於查詢參數不同。兩次讀取間剛收盘 K 的更新，才是本次觀察到的差異。

## 修正

僅把合約每個 5 分鐘週期的取資料起點由收盤後 5 秒改為 15 秒；現貨仍為 5 秒。15 秒根據上述 10 秒後已一致的觀測，加上 5 秒餘量，屬待持續驗證的資料可用率修正，不保證供應端以後不再更新。

主程序、下一次研究時間顯示與健康檢查引用相同模式等待值。已領取週期仍持久化，重啟不重跑、不追補突發下單；原最晚領取窗口不延長，模型／native 的交易所首分鐘截止亦不變。每次仍嚴格比對 OHLCV、hash、模型身份與來源，不忽略 volume、不接受不一致資料。固定模型權重與 pin 保留，沒有重訓、放大輸出或重設品質診斷。

原 forecast-net-edge-v1 成本門檻、動量、風險、持倉上限、既有退出計畫及兩個交易目標完整保留。資料可用率改善不表示一定出現進場訊號；現貨品質抑制是另一項已知限制。

驗證：486 項 JS、517 項 Python、138 份語法／設定檢查通過；隔離 native smoke 完成模擬開平倉，realOrders=0。實際部署、載入來源、原計畫與本金保留、首次新週期起點及模型覆蓋率，以 `local/v12-delay-2026-09-15/running.json` 和 `validation.json` 為準，檔案尚未存在時不算完成啟動。

部署後已於 UTC 18:18:40 完成驗證：首次合約週期在 18:15:15.023 開始、18:15:24.932 完成，取得 4/4 預測且無模型錯誤；現貨 18:15:05.011 開始，取得 10/10 預測但模型品質審查仍抑制進場。兩邊皆 HOLD，不能把資料完整等同成交。66 份載入來源核對通過，原模型在這次維護中持續運行，沒有漏掉預定 5 分鐘邊界。18:18 的實際帳目仍為新版各 100 目標各 0 筆、兩模式皆無持倉；原 v12 累計淨損仍為 5.44541199 USDT。每 15 分鐘既有監控持續追蹤成交、資料缺失及兩小時未成交原因，詳見本次 final-audit.json。
